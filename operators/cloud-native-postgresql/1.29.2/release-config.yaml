---
merge: true
catalog_templates:
  - template_name: catalog.yaml
    channels:
      - stable-v1.29
    replaces: cloud-native-postgresql.v1.29.1
  - template_name: catalog.yaml
    channels:
      - fast
    replaces: cloud-native-postgresql.v1.29.1

apiVersion: v1
data:
  schema: |
    {
      "namespace": "io.confluent.examples.clients.basicavro",
      "type": "record",
      "name": "Payment",
      "fields": [
          {"name": "id", "type": "string"}
      ]
    }
kind: ConfigMap
metadata:
  name: schema-config

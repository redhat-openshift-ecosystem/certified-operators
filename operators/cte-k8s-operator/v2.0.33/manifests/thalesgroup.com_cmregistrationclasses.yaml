apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/instance: cte-cos
    app.kubernetes.io/name: cte-cos
  name: cmregistrationclasses.thalesgroup.com
spec:
  group: thalesgroup.com
  names:
    kind: CMregistrationClass
    plural: cmregistrationclasses
    shortNames:
    - cmrc
    singular: cmregistrationclass
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              default_replicas:
                description: Number of replicas (modifiable)
                format: int32
                type: integer
              external_ca_secret:
                description: Name of Kubernetes Secret containing external CA as Squid
                  CA Certificate (immutable)
                type: string
              memory_limit:
                description: Memory limit for processor container (e.g., '2Gi', '4Gi').
                  Defaults to 2Gi. Logger is hardcoded to 256Mi.
                pattern: ^([0-9.]+)([eEinumkKMGTP]*[-+]?[0-9]*)$
                type: string
              memory_request:
                description: Memory request for processor container (e.g., '1Gi',
                  '2Gi'). Defaults to 2Gi. Logger is hardcoded to 256Mi.
                pattern: ^([0-9.]+)([eEinumkKMGTP]*[-+]?[0-9]*)$
                type: string
              nodeport_service:
                description: NodePort for external traffic. Use 'auto' for Kubernetes
                  auto-assignment, or specify a port number (e.g., '30080'). (Immutable)
                type: string
              reg_secret:
                description: Name of Kubernetes Secret used for registration (immutable)
                type: string
              server_addr:
                description: Address of the registration server (modifiable)
                type: string
              session_timeout:
                description: Configure session affinity timeout in seconds (e.g.,
                  1800, 3600) for ClusterIP and NodePort services. Default session
                  affinity is configured with 900 seconds.
                format: int32
                type: integer
              storage_cred:
                description: Name of Kubernetes Secret containing AWS credentials
                  (immutable)
                type: string
              storage_group:
                description: Immutable service group identifier
                type: string
            required:
            - server_addr
            - storage_group
            - reg_secret
            - storage_cred
            - default_replicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

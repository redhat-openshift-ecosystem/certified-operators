apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: edb-hcp-operator
    control-plane: edb-hcp-operator
  name: hmregistries.edbpgai.edb.com
spec:
  group: edbpgai.edb.com
  names:
    kind: HMRegistry
    listKind: HMRegistryList
    plural: hmregistries
    singular: hmregistry
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The current phase of the HMRegistry
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Additional information about the HMRegistry's state
      jsonPath: .status.message
      name: Message
      type: string
    - description: The URI for the container runtime interface (CRI) to access the
        HMRegistry
      jsonPath: .status.uris.cri
      name: CRI URI
      type: string
    - description: The URI for Kubernetes components to access the HMRegistry
      jsonPath: .status.uris.kube
      name: Kube URI
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HMRegistry is the Schema for the hmregistries API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of HMRegistry
            properties:
              mode:
                default: Insecure
                description: Mode specifies the access mode for the HMRegistry. If
                  not specified, it defaults to "Insecure".
                enum:
                - Insecure
                type: string
              resources:
                description: |-
                  Resources defines the resource requirements for the HMRegistry deployment.
                  If not specified, it will use the default resource requests and limits defined in the operator.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: Storage defines the storage configuration for the HMRegistry.
                properties:
                  pvc:
                    description: PVC specifies the configuration for PersistentVolumeClaim
                      storage.
                    properties:
                      className:
                        description: |-
                          ClassName specifies the storage class to use for the PersistentVolumeClaim.
                          If not specified, the default storage class will be used.
                        type: string
                      size:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Size specifies the size of the PersistentVolumeClaim.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    type: object
                type: object
            required:
            - storage
            type: object
          status:
            description: status defines the observed state of HMRegistry
            properties:
              message:
                description: Message provides additional information about the current
                  state of the HMRegistry, especially in case of errors.
                type: string
              observedGeneration:
                description: ObservedGeneration is the last spec generation reconciled
                  into status.
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the HMRegistry.
                type: string
              uris:
                description: URIs provides the URIs for accessing the HMRegistry.
                properties:
                  cri:
                    description: CRI defines the URI for the container runtime interface
                      (CRI) to access the HMRegistry.
                    type: string
                  kube:
                    description: Kube defines the URI for Kubernetes components to
                      access the HMRegistry.
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

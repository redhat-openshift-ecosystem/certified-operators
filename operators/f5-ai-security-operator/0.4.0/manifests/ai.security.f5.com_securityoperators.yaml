apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: securityoperators.ai.security.f5.com
spec:
  group: ai.security.f5.com
  names:
    kind: SecurityOperator
    listKind: SecurityOperatorList
    plural: securityoperators
    singular: securityoperator
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              jobManager:
                properties:
                  enabled:
                    type: boolean
                  namespace:
                    type: string
                  values:
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    type: string
                required:
                - enabled
                type: object
              moderator:
                properties:
                  enabled:
                    type: boolean
                  namespace:
                    type: string
                  values:
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    type: string
                required:
                - enabled
                type: object
              postgresql:
                properties:
                  enabled:
                    type: boolean
                  namespace:
                    type: string
                  values:
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    type: string
                required:
                - enabled
                type: object
              redTeam:
                properties:
                  enabled:
                    type: boolean
                  namespace:
                    type: string
                  values:
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    type: string
                required:
                - enabled
                type: object
              registryAuth:
                description: RegistryAuthSpec defines the configuration for container
                  registry authentication.
                properties:
                  enabled:
                    default: true
                    description: Enabled determines whether registry authentication
                      should be configured.
                    type: boolean
                  existingSecret:
                    description: |-
                      ExistingSecret references an existing secret in the operator namespace to copy.
                      If specified, Username and Password are ignored.
                    type: string
                  password:
                    description: Password for registry authentication (used when ExistingSecret
                      is not specified).
                    type: string
                  registry:
                    default: harbor.calypsoai.app
                    description: Registry is the container registry server (e.g.,
                      docker.io, ghcr.io, harbor.example.com).
                    type: string
                  secretName:
                    default: regcred
                    description: SecretName is the name of the secret to create in
                      product namespaces.
                    type: string
                  username:
                    description: Username for registry authentication (used when ExistingSecret
                      is not specified).
                    type: string
                required:
                - enabled
                type: object
              scanner:
                properties:
                  enabled:
                    type: boolean
                  namespace:
                    type: string
                  values:
                    x-kubernetes-preserve-unknown-fields: true
                  version:
                    type: string
                required:
                - enabled
                type: object
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jobManager:
                description: |-
                  ProductStatus records what the operator last successfully applied for a product.
                  This is the operator's durable ledger for version reconciliation + drift detection.
                properties:
                  appliedChartVersion:
                    type: string
                  appliedSpecHash:
                    type: string
                type: object
              moderator:
                description: |-
                  ProductStatus records what the operator last successfully applied for a product.
                  This is the operator's durable ledger for version reconciliation + drift detection.
                properties:
                  appliedChartVersion:
                    type: string
                  appliedSpecHash:
                    type: string
                type: object
              postgresql:
                description: |-
                  ProductStatus records what the operator last successfully applied for a product.
                  This is the operator's durable ledger for version reconciliation + drift detection.
                properties:
                  appliedChartVersion:
                    type: string
                  appliedSpecHash:
                    type: string
                type: object
              redTeam:
                description: |-
                  ProductStatus records what the operator last successfully applied for a product.
                  This is the operator's durable ledger for version reconciliation + drift detection.
                properties:
                  appliedChartVersion:
                    type: string
                  appliedSpecHash:
                    type: string
                type: object
              scanner:
                description: |-
                  ProductStatus records what the operator last successfully applied for a product.
                  This is the operator's durable ledger for version reconciliation + drift detection.
                properties:
                  appliedChartVersion:
                    type: string
                  appliedSpecHash:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

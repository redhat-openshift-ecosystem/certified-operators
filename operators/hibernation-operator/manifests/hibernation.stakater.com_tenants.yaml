apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  creationTimestamp: null
  name: tenants.hibernation.stakater.com
spec:
  group: hibernation.stakater.com
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec defines the desired state of Tenant
            properties:
              accessControl:
                description: AccessControl defines the list of admins, editors and
                  viewers
                properties:
                  editors:
                    description: editors represents the list of editors
                    properties:
                      groups:
                        description: Groups defines the list of linked Openshift Groups
                        items:
                          type: string
                        type: array
                      users:
                        description: Users defines the list of linked Openshift Users
                        items:
                          type: string
                        type: array
                    type: object
                  owners:
                    description: owners represents the list of owners
                    properties:
                      groups:
                        description: Groups defines the list of linked Openshift Groups
                        items:
                          type: string
                        type: array
                      users:
                        description: Users defines the list of linked Openshift Users
                        items:
                          type: string
                        type: array
                    type: object
                  viewers:
                    description: viewers represents the list of viewers
                    properties:
                      groups:
                        description: Groups defines the list of linked Openshift Groups
                        items:
                          type: string
                        type: array
                      users:
                        description: Users defines the list of linked Openshift Users
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              desc:
                description: Desc can contains description about the tenant
                type: string
              hibernation:
                description: Hibernation contains the schedule during which the tenant
                  will be sleeping
                properties:
                  sleepSchedule:
                    description: SleepSchedule specifies the cron schedule for resource
                      hibernation. It must follow the standard cron format
                    type: string
                  wakeSchedule:
                    description: |-
                      WakeSchedule specifies the cron schedule for waking up resources. It must follow the standard cron format awoken
                      It is optional and if not provided, resources will stay asleep
                    type: string
                type: object
              hostValidationConfig:
                description: HostValidationConfig defines the allowed ingress and
                  route host names for the tenant
                properties:
                  allowed:
                    description: |-
                      Allowed is a list of allowed host names
                      If specified, host names must be in this list to be allowed
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  allowedRegex:
                    description: |-
                      AllowedRegex is a regular expression that defines the allowed host names
                      If specified, host names must match this regex to be allowed
                    type: string
                  denyWildcards:
                    default: false
                    description: |-
                      DenyWildcards indicates whether wildcard host names are allowed or not
                      If true, wildcard host names are not allowed
                      If false, wildcard host names are allowed
                    type: boolean
                type: object
              imageRegistries:
                properties:
                  allowed:
                    description: Allowed represents a list of allowed values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  denied:
                    description: Denied represents a list of denied values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              ingressClasses:
                properties:
                  allowed:
                    description: Allowed represents a list of allowed values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  denied:
                    description: Denied represents a list of denied values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              namespaces:
                description: namespaces defines namespaces and their metadata
                properties:
                  metadata:
                    description: metadata applies given labels and annotations
                    properties:
                      common:
                        description: commonmetadata applies given labels and annotations
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations to be added to all tenant's namespaces
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels to be added to all tenant's namespaces
                            type: object
                        type: object
                      sandbox:
                        description: sandboxmetadata applies given labels and annotation
                          across sandbox namespaces
                        properties:
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations to be added to all tenant's namespaces
                            type: object
                          labels:
                            additionalProperties:
                              type: string
                            description: Labels to be added to all tenant's namespaces
                            type: object
                        type: object
                      specific:
                        description: specificmetadata applies given labels and annotation
                          across specific namespaces
                        items:
                          description: labels and annotations for a specific list
                            of namespaces
                          properties:
                            annotations:
                              additionalProperties:
                                type: string
                              description: Annotations to be added to the specified
                                namespaces
                              type: object
                            labels:
                              additionalProperties:
                                type: string
                              description: Labels to be added to the specified namespaces
                              type: object
                            namespaces:
                              description: Namespaces names on which labels and annotations
                                will be applied
                              items:
                                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])$
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  onDeletePurgeNamespaces:
                    default: false
                    description: ondeletepurgenamespaces is used to enable or disable
                      the namespace purge feature
                    type: boolean
                  sandboxes:
                    description: sandboxes is used to enable or disable the sandbox
                      feature
                    properties:
                      enabled:
                        description: enabled is used to enable or disable the sandbox
                          feature
                        type: boolean
                      private:
                        description: private is used to enable or disable the private
                          sandbox feature
                        type: boolean
                    type: object
                  withTenantPrefix:
                    description: WithTenantPrefix will create new namespaces mentioned
                      in it and add a prefix of the tenant name to them
                    items:
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])$
                      type: string
                    type: array
                  withoutTenantPrefix:
                    description: WithoutTenantPrefix will create new namespaces mentioned
                      in it
                    items:
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])$
                      type: string
                    type: array
                type: object
              podPriorityClasses:
                properties:
                  allowed:
                    description: Allowed represents a list of allowed values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  denied:
                    description: Denied represents a list of denied values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              quota:
                description: Quota field is used to link relevant Tenant Operator
                  Quota CR
                type: string
              serviceAccounts:
                properties:
                  allowed:
                    description: Allowed represents a list of allowed values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  denied:
                    description: Denied represents a list of denied values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
              storageClasses:
                properties:
                  allowed:
                    description: Allowed represents a list of allowed values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                  denied:
                    description: Denied represents a list of denied values for the
                      tenant
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: atomic
                type: object
            required:
            - quota
            type: object
          status:
            description: TenantStatus defines the observed state of Tenant
            properties:
              conditions:
                description: Status conditions for tenant
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deployedNamespaces:
                description: DeployedNamespaces has the string for created namespaces
                  so they can be synced with spec
                items:
                  type: string
                type: array
              deployedSandboxes:
                additionalProperties:
                  type: string
                description: DeployedSandboxes has the map for created sandbox environments
                  so they can be synced with spec
                type: object
              namespaces:
                description: namespacesStatus stores the previous state of namespaces,
                  if mentioned in spec
                properties:
                  commonStatus:
                    description: CommonStatus stores the previous state of labels
                      and annotation applied across all tenant namespaces, if mentioned
                      in spec
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to be added to all tenant's namespaces
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to be added to all tenant's namespaces
                        type: object
                    type: object
                  sandboxStatus:
                    description: SandboxStatus stores the previous state of labels
                      and annotation applied across all sandbox namespaces, if mentioned
                      in spec
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to be added to all tenant's namespaces
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to be added to all tenant's namespaces
                        type: object
                    type: object
                  specificStatus:
                    description: SpecificStatus stores the previous state of labels
                      and annotations applied across specific tenant namespaces, if
                      mentioned in spec
                    items:
                      description: labels and annotations for a specific list of namespaces
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          description: Annotations to be added to the specified namespaces
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          description: Labels to be added to the specified namespaces
                          type: object
                        namespaces:
                          description: Namespaces names on which labels and annotations
                            will be applied
                          items:
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])$
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              sandboxState:
                description: SandboxState stores the previous state of sandbox field,
                  if mentioned in spec
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

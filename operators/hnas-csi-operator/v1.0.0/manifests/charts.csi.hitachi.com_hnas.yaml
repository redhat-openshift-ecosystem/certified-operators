apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: hnas.charts.csi.hitachi.com
spec:
  group: charts.csi.hitachi.com
  names:
    kind: HNAS
    listKind: HNASList
    plural: hnas
    singular: hnas
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: HNAS is the Schema for the hnas API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Defines the desired state of the HNAS CSI driver
            properties:
              controller:
                description: Overwites parameters of the deployment hnas-csi-controller.
                properties:
                  csiProvisioner:
                    description: Overwrites the csi-provisioner details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  csiResizer:
                    description: Overwrites the csi-resizer details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  csiSnapshotter:
                    description: Overwrites the csi-snapshotter details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  externalAttacher:
                    description: Overwrites the external-attacher details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  hnasDriver:
                    description: Overwrites the hnas-driver details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  livenessProbe:
                    description: Overwrites the liveness-probe details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                type: object
              node:
                description: Overwites parameters of the deployment hnas-csi-node.
                properties:
                  driverRegistrar:
                    description: Overwrites the driver-registrar details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  hnasDriver:
                    description: Overwrites the hnas-driver details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                  livenessProbe:
                    description: Overwrites the liveness-probe details
                    properties:
                      args:
                        description: |-
                          Arguments to the entrypoint.
                          It replaces all parameters that have already been set as default.
                        items:
                          type: string
                        type: array
                      image:
                        description: Image download location, including tag
                        type: string
                      imagePullPolicy:
                        description: Image pull policy - will default to Always if
                          image :latest tag is specified
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                    type: object
                type: object
            type: object
          status:
            description: Status defines the observed state of HNAS
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: contain details for one aspect of the current state
                    of this API Resource
                  properties:
                    lastTransitionTime:
                      description: time the condition transitioned from one status
                        to another
                      format: date-time
                      type: string
                    reason:
                      description: programmatic identifier indicating the reason for
                        the condition's last transition
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition
                      type: string
                  type: object
                type: array
                x-kubernetes-preserve-unknown-fields: true
              deployedRelease:
                description: Describes details of the resource that has been deployed
                  when the object was created
                properties:
                  manifest:
                    description: Contians the maifest file that was used to create
                      the HNAS CSI driver resource
                    type: string
                  name:
                    description: name of the deployed ressource
                    type: string
                type: object
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

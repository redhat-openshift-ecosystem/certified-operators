# Intel OBL Limited Distribution (Commercial Use) License Agreement [v2022.12.20]
# Copyright (c) 2025 Intel Corporation
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  creationTimestamp: null
  name: cpupowerprovs.ipm.intel.com
spec:
  group: ipm.intel.com
  names:
    kind: CPUPowerProv
    listKind: CPUPowerProvList
    plural: cpupowerprovs
    singular: cpupowerprov
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CPUPowerProv is the Schema for the cpupowerprovs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CPUPowerProvSpec defines the desired state of CPUPowerProv
            properties:
              cstates:
                description: C-States configuration for CPU power management
                properties:
                  cores:
                    additionalProperties:
                      additionalProperties:
                        type: boolean
                      type: object
                    type: object
                  idleGovernor:
                    type: string
                type: object
              pstates:
                description: P-States configuration for CPU power management
                properties:
                  cores:
                    additionalProperties:
                      description: CorePstateAttrs defines the attributes for the
                        cores section of the pstates struct
                      properties:
                        energyPerformanceBias:
                          type: string
                        energyPerformancePreference:
                          type: string
                        freqGovernor:
                          type: string
                        maxFreqMHz:
                          type: string
                        minFreqMHz:
                          type: string
                      type: object
                    type: object
                  driverStatus:
                    type: string
                  global:
                    description: Global defines the global struct of the pstates struct
                    properties:
                      conservative:
                        description: Conservative defines the struct for the conservative
                          section of the global struct
                        properties:
                          downThreshold:
                            maximum: 100
                            minimum: 0
                            type: integer
                          freqStep:
                            maximum: 100
                            minimum: 0
                            type: integer
                          ignoreNiceLoad:
                            maximum: 1
                            minimum: 0
                            type: integer
                          samplingDownFactor:
                            maximum: 100
                            minimum: 0
                            type: integer
                          samplingRate:
                            minimum: 0
                            type: integer
                          upThreshold:
                            maximum: 100
                            minimum: 0
                            type: integer
                        type: object
                      maxPerfPct:
                        maximum: 100
                        minimum: 0
                        type: integer
                      minPerfPct:
                        maximum: 100
                        minimum: 0
                        type: integer
                      ondemand:
                        description: Ondemand defines the struct for the ondemand
                          section of the global struct
                        properties:
                          ignoreNiceLoad:
                            maximum: 1
                            minimum: 0
                            type: integer
                          ioIsBusy:
                            maximum: 1
                            minimum: 0
                            type: integer
                          powersaveBias:
                            maximum: 1000
                            minimum: 0
                            type: integer
                          samplingDownFactor:
                            maximum: 100000
                            minimum: 1
                            type: integer
                          samplingRate:
                            minimum: 0
                            type: integer
                          upThreshold:
                            maximum: 100
                            minimum: 0
                            type: integer
                        type: object
                      schedutil:
                        description: Schedutil defines the struct for the schedutil
                          section of the global struct
                        properties:
                          rateLimitUs:
                            minimum: 0
                            type: integer
                        type: object
                      turbo:
                        type: boolean
                    type: object
                type: object
              restConfig:
                description: RestConfig defines the IPMClientConfigType struct of
                  the CPUPowerProv
                properties:
                  ca:
                    type: string
                  cert:
                    type: string
                  clientTimeout:
                    type: integer
                  dialTimeout:
                    type: integer
                  endpoint:
                    type: string
                  key:
                    type: string
                type: object
              uncoreFreqCPU:
                description: Uncore frequency configuration for CPU power management
                properties:
                  cpu0:
                    items:
                      properties:
                        maxFreqMHz:
                          type: string
                        minFreqMHz:
                          type: string
                      type: object
                    type: array
                  cpu1:
                    items:
                      properties:
                        maxFreqMHz:
                          type: string
                        minFreqMHz:
                          type: string
                      type: object
                    type: array
                type: object
              uncoreFreqIO:
                description: Uncore frequency configuration for IO power management
                properties:
                  cpu0:
                    items:
                      properties:
                        maxFreqMHz:
                          type: string
                        minFreqMHz:
                          type: string
                      type: object
                    type: array
                  cpu1:
                    items:
                      properties:
                        maxFreqMHz:
                          type: string
                        minFreqMHz:
                          type: string
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: CPUPowerProvStatus defines the observed state of CPUPowerProv
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the CPUPowerProv state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this CPUPowerProv
                format: int64
                type: integer
            required:
            - conditions
            - observedGeneration
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

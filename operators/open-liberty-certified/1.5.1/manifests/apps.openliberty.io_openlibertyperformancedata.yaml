apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  creationTimestamp: null
  labels:
    app.kubernetes.io/instance: open-liberty-operator
    app.kubernetes.io/managed-by: olm
    app.kubernetes.io/name: open-liberty-operator
  name: openlibertyperformancedata.apps.openliberty.io
spec:
  group: apps.openliberty.io
  names:
    kind: OpenLibertyPerformanceData
    listKind: OpenLibertyPerformanceDataList
    plural: openlibertyperformancedata
    shortNames:
    - olperfdata
    singular: openlibertyperformancedata
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Indicates if performance data operation has started
      jsonPath: .status.conditions[?(@.type=='Started')].status
      name: Started
      type: string
    - description: Reason for performance data operation failing to start
      jsonPath: .status.conditions[?(@.type=='Started')].reason
      name: Reason
      priority: 1
      type: string
    - description: Message for performance data operation failing to start
      jsonPath: .status.conditions[?(@.type=='Started')].message
      name: Message
      priority: 1
      type: string
    - description: Indicates if performance data operation has completed
      jsonPath: .status.conditions[?(@.type=='Completed')].status
      name: Completed
      type: string
    - description: Reason for performance data operation failing to complete
      jsonPath: .status.conditions[?(@.type=='Completed')].reason
      name: Reason
      priority: 1
      type: string
    - description: Message for performance data operation failing to complete
      jsonPath: .status.conditions[?(@.type=='Completed')].message
      name: Message
      priority: 1
      type: string
    - description: Indicates filename of the server performance data
      jsonPath: .status.performanceDataFile
      name: Performance Data file
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Day-2 operation for generating server performance data
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OpenLibertyPerformanceDataSpec defines the desired state
              of OpenLibertyPerformanceData
            properties:
              interval:
                description: The time, in seconds, between executions. The minimum
                  value is 1 second. Defaults to 30 seconds.
                minimum: 1
                type: integer
              podName:
                description: The name of the Pod, which must be in the same namespace
                  as the OpenLibertyPerformanceData CR.
                type: string
              timespan:
                description: The total time, in seconds, for gathering performance
                  data. The minimum value is 10 seconds. The maximum value is 600
                  seconds (10 minutes). Defaults to 240 seconds (4 minutes).
                maximum: 600
                minimum: 10
                type: integer
            required:
            - podName
            type: object
          status:
            description: Defines the observed state of OpenLibertyPerformanceData
            properties:
              conditions:
                items:
                  description: OperationStatusCondition ...
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    lastUpdateTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      description: OperationStatusConditionType ...
                      type: string
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              observedGeneration:
                description: The generation identifier of this OpenLibertyPerformanceData
                  instance completely reconciled by the Operator.
                format: int64
                type: integer
              performanceDataFile:
                description: Location of the generated performance data file
                type: string
              versions:
                properties:
                  reconciled:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

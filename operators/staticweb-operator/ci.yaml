---
# Red Hat Partner Connect Operator bundle component PID.
# Replace this with the project ID from https://connect.redhat.com
# (Components tab of your Operator listing).
cert_project_id: 6ab5768fed137bc18ce852e4

# Do not auto-merge the first certification until checks pass and PID is set.
merge: false

operator_path: "operators/staticweb-operator"
bundle_path: "operators/staticweb-operator/0.0.2"

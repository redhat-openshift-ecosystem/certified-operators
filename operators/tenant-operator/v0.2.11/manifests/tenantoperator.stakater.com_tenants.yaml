apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: tenants.tenantoperator.stakater.com
spec:
  group: tenantoperator.stakater.com
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec defines the desired state of Tenant
            properties:
              desc:
                type: string
              namespacetemplate:
                description: NamespaceTemplate dynamically adds properties to namespaces
                properties:
                  templateInstances:
                    description: TemplateInstances are templates that will be added
                      in matching namespaces
                    items:
                      description: TenantTemplateInstanceTemplate defines a template
                        instance template
                      properties:
                        metadata:
                          description: The metadata of the template instace to create
                          type: object
                        selector:
                          description: The labeSelector to filter spaces that templates
                            should be created by default in. If it is omitted, templates
                            are created in all spaces.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: A label selector requirement is a selector
                                  that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: operator represents a key's relationship
                                      to a set of values. Valid operators are In,
                                      NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: values is an array of string values.
                                      If the operator is In or NotIn, the values array
                                      must be non-empty. If the operator is Exists
                                      or DoesNotExist, the values array must be empty.
                                      This array is replaced during a strategic merge
                                      patch.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: matchLabels is a map of {key,value} pairs.
                                A single {key,value} in the matchLabels map is equivalent
                                to an element of matchExpressions, whose key field
                                is "key", the operator is "In", and the values array
                                contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                        spec:
                          description: The spec of the template instance
                          properties:
                            parameters:
                              description: Parameters hold the values of the defined
                                parameters in the template
                              items:
                                properties:
                                  name:
                                    description: Name is the name of the parameter
                                      to set
                                    type: string
                                  value:
                                    description: Value is the value of the parameter
                                      to set
                                    type: string
                                type: object
                              type: array
                            sync:
                              description: If true the template instance will keep
                                the deployed resources in sync with the template.
                              type: boolean
                            template:
                              description: The template to instantiate. This is an
                                immutable field
                              type: string
                          required:
                          - template
                          type: object
                      type: object
                    type: array
                type: object
              quota:
                type: string
              users:
                description: Users defines the list of users of Tenant
                properties:
                  edit:
                    items:
                      type: string
                    type: array
                  owner:
                    items:
                      type: string
                    type: array
                  view:
                    items:
                      type: string
                    type: array
                type: object
            required:
            - users
            type: object
          status:
            description: TenantStatus defines the observed state of Tenant
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []

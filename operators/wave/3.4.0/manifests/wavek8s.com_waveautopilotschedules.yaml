apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  creationTimestamp: null
  name: waveautopilotschedules.wavek8s.com
spec:
  group: wavek8s.com
  names:
    kind: WaveAutopilotSchedule
    plural: waveautopilotschedules
    singular: waveautopilotschedule
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WaveAutopilotScheduleSpec via
          `CustomResource`
        properties:
          spec:
            properties:
              enabled:
                type: boolean
              mappings:
                default: []
                items:
                  properties:
                    preset:
                      description: Name of a `WaveAutopilotPreset` CR. Resolved to
                        its `uuidv5(name)` config id.
                      type: string
                    targets:
                      items:
                        properties:
                          name:
                            type: string
                          namespace:
                            type: string
                        required:
                        - name
                        - namespace
                        type: object
                      type: array
                  required:
                  - preset
                  - targets
                  type: object
                type: array
              timing:
                type: object
                x-kubernetes-preserve-unknown-fields: true
            required:
            - timing
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Shared status condition shape — mirrors metav1.Condition.
                    Used by all 15 `WaveXStatus` structs (added in Tasks 6-8).

                    No `Default` derive on purpose: an all-empty-string condition (empty `type`/
                    `status`/`reason`) is never valid, so we never want one constructed implicitly.
                    `WaveXStatus` structs default their `conditions` to an empty `Vec`, which does
                    not require `WaveCondition: Default`.
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: WaveAutopilotSchedule
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

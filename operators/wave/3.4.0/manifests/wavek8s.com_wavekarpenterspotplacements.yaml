apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  creationTimestamp: null
  name: wavekarpenterspotplacements.wavek8s.com
spec:
  group: wavek8s.com
  names:
    kind: WaveKarpenterSpotPlacement
    plural: wavekarpenterspotplacements
    singular: wavekarpenterspotplacement
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WaveKarpenterSpotPlacementSpec
          via `CustomResource`
        properties:
          spec:
            properties:
              enabled:
                type: boolean
              placementStrategy:
                description: |-
                  Mirrors `PlacementStrategy` DB enum (e.g. `prefer | require | ...`).
                  Free string at CRD layer; reconciler parses via FromStr.
                type: string
              podSplitThreshold:
                format: int32
                type: integer
              targetRef:
                properties:
                  kind:
                    description: '`Deployment` | `Rollout` | `DeploymentConfig`.'
                    type: string
                  name:
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - placementStrategy
            - podSplitThreshold
            - targetRef
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Shared status condition shape — mirrors metav1.Condition.
                    Used by all 15 `WaveXStatus` structs (added in Tasks 6-8).

                    No `Default` derive on purpose: an all-empty-string condition (empty `type`/
                    `status`/`reason`) is never valid, so we never want one constructed implicitly.
                    `WaveXStatus` structs default their `conditions` to an empty `Vec`, which does
                    not require `WaveCondition: Default`.
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: WaveKarpenterSpotPlacement
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

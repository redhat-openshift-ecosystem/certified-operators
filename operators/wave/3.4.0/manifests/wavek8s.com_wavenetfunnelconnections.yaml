apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  creationTimestamp: null
  name: wavenetfunnelconnections.wavek8s.com
spec:
  group: wavek8s.com
  names:
    kind: WaveNetfunnelConnection
    plural: wavenetfunnelconnections
    singular: wavenetfunnelconnection
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WaveNetfunnelConnectionSpec via
          `CustomResource`
        properties:
          spec:
            properties:
              clientIdSecretRef:
                properties:
                  key:
                    type: string
                  name:
                    type: string
                required:
                - key
                - name
                type: object
              enabled:
                type: boolean
              host:
                type: string
              nfApiType:
                description: |-
                  NetFUNNEL API type. Maps to `NfType` enum in
                  `core/types/src/models/external_api_configs.rs`.
                type: string
              organizationId:
                type: string
              secretKeySecretRef:
                properties:
                  key:
                    type: string
                  name:
                    type: string
                required:
                - key
                - name
                type: object
              tenantId:
                type: string
            required:
            - clientIdSecretRef
            - host
            - nfApiType
            - organizationId
            - secretKeySecretRef
            - tenantId
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Shared status condition shape — mirrors metav1.Condition.
                    Used by all 15 `WaveXStatus` structs (added in Tasks 6-8).

                    No `Default` derive on purpose: an all-empty-string condition (empty `type`/
                    `status`/`reason`) is never valid, so we never want one constructed implicitly.
                    `WaveXStatus` structs default their `conditions` to an empty `Vec`, which does
                    not require `WaveCondition: Default`.
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: WaveNetfunnelConnection
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

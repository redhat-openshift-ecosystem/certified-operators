apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  creationTimestamp: null
  name: wavenetfunnelmappings.wavek8s.com
spec:
  group: wavek8s.com
  names:
    kind: WaveNetfunnelMapping
    plural: wavenetfunnelmappings
    singular: wavenetfunnelmapping
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WaveNetfunnelMappingSpec via
          `CustomResource`
        properties:
          spec:
            properties:
              nfMapping:
                default: []
                items:
                  properties:
                    isSection:
                      default: false
                      type: boolean
                    nfProjectId:
                      type: string
                    nfSegmentId:
                      type: string
                  required:
                  - nfProjectId
                  - nfSegmentId
                  type: object
                type: array
              rule:
                description: |-
                  Mirrors `K8sDeploymentNfRule` minus system fields (id, deployment_id,
                  created/updated_at, nf_mapping — that field is at the spec level here).
                properties:
                  cooldownPeriodSecs:
                    format: int64
                    nullable: true
                    type: integer
                  nfEnabled:
                    type: boolean
                  ruleCpu:
                    format: double
                    nullable: true
                    type: number
                  ruleMaxReplicas:
                    nullable: true
                    type: boolean
                  ruleMemory:
                    format: double
                    nullable: true
                    type: number
                required:
                - nfEnabled
                type: object
              targetRef:
                properties:
                  kind:
                    description: '`Deployment` | `Rollout` | `DeploymentConfig`.'
                    type: string
                  name:
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - rule
            - targetRef
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Shared status condition shape — mirrors metav1.Condition.
                    Used by all 15 `WaveXStatus` structs (added in Tasks 6-8).

                    No `Default` derive on purpose: an all-empty-string condition (empty `type`/
                    `status`/`reason`) is never valid, so we never want one constructed implicitly.
                    `WaveXStatus` structs default their `conditions` to an empty `Vec`, which does
                    not require `WaveCondition: Default`.
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: WaveNetfunnelMapping
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    helm.sh/resource-policy: keep
  creationTimestamp: null
  name: wavelabelgroups.wavek8s.com
spec:
  group: wavek8s.com
  names:
    kind: WaveLabelGroup
    plural: wavelabelgroups
    singular: wavelabelgroup
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for WaveLabelGroupSpec via `CustomResource`
        properties:
          spec:
            properties:
              context:
                description: |-
                  Mirrors `types::models::label_group::LabelGroupContext`.
                  snake_case serde matches the DB-side Display/FromStr representation.
                enum:
                - cluster_overview
                - workload_report
                type: string
              description:
                nullable: true
                type: string
              labelFilters:
                description: Mirrors `types::models::label_group::LabelFilters`.
                nullable: true
                properties:
                  conditions:
                    items:
                      description: |-
                        Mirrors `types::models::label_group::LabelCondition`.
                        `operator` is kept as `String` at the CRD layer (free-form;
                        DB-side `LabelOperator` parses case-insensitively at reconcile time).
                      properties:
                        key:
                          type: string
                        operator:
                          description: '`"equals"` | `"in"` | `"exists"` — mirrors
                            `LabelOperator` DB enum.'
                          type: string
                        value:
                          nullable: true
                          type: string
                        values:
                          items:
                            type: string
                          nullable: true
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  logic:
                    default: ""
                    description: '`"and"` | `"or"` — combinator logic for conditions.'
                    type: string
                required:
                - conditions
                type: object
              namespaceFilters:
                items:
                  type: string
                nullable: true
                type: array
            required:
            - context
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  description: |-
                    Shared status condition shape — mirrors metav1.Condition.
                    Used by all 15 `WaveXStatus` structs (added in Tasks 6-8).

                    No `Default` derive on purpose: an all-empty-string condition (empty `type`/
                    `status`/`reason`) is never valid, so we never want one constructed implicitly.
                    `WaveXStatus` structs default their `conditions` to an empty `Vec`, which does
                    not require `WaveCondition: Default`.
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                nullable: true
                type: integer
            type: object
        required:
        - spec
        title: WaveLabelGroup
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null

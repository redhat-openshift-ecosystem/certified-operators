cert_project_id: "695d06a2c9b42952f192d980"

# Required by hosted operator pipeline: where the operator lives in the repo
operator_path: "operators/webserver-operator"

# Which bundle/version directory to validate and submit
bundle_path: "operators/webserver-operator/1.0.35"

# Channel metadata (matches your annotations.yaml / CSV annotations)
channels:
  - "stable"
default_channel: "stable"
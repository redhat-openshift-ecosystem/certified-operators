apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  creationTimestamp: null
  name: zabbixapimigrations.operator.zabbix.com
spec:
  group: operator.zabbix.com
  names:
    kind: ZabbixAPIMigration
    listKind: ZabbixAPIMigrationList
    plural: zabbixapimigrations
    singular: zabbixapimigration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceRef.kind
      name: Kind
      type: string
    - jsonPath: .spec.sourceRef.name
      name: Source
      type: string
    - jsonPath: .spec.desiredState
      name: Desired
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.step
      name: Step
      type: string
    - jsonPath: .status.transferredResources
      name: Owned
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: |-
          ZabbixAPIMigration coordinates a safe migration of one legacy v1alpha1 CR to
          the canonical v1beta1 API without recreating its managed workloads.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ZabbixAPIMigrationSpec defines one explicitly authorized alpha-to-beta
              migration. Each object is migrated independently to keep the blast radius
              small and rollback deterministic.
            properties:
              desiredState:
                default: Prepared
                description: |-
                  DesiredState controls the irreversible commit. Prepared freezes alpha and
                  creates a paused beta object without changing workload ownership. Finalized
                  starts a forward-only ownership transfer and removes alpha. RolledBack is
                  accepted only before the controller enters Committing.
                enum:
                - Prepared
                - Finalized
                - RolledBack
                type: string
              sourceRef:
                description: |-
                  APIMigrationSourceReference identifies one legacy object in the migration's
                  namespace. The API groups and versions are fixed by this API contract.
                properties:
                  kind:
                    description: Kind is the Zabbix resource kind to migrate.
                    enum:
                    - ZabbixAgent
                    - ZabbixAgent2
                    - ZabbixFull
                    - ZabbixProxyMysql
                    - ZabbixProxySqlite
                    - ZabbixServer
                    type: string
                  name:
                    description: Name is the legacy object's name.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - sourceRef
            type: object
            x-kubernetes-validations:
            - message: sourceRef is immutable
              rule: self.sourceRef == oldSelf.sourceRef
          status:
            description: ZabbixAPIMigrationStatus reports durable migration checkpoints.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastTransitionTime:
                format: date-time
                type: string
              managedResources:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  APIMigrationPhase describes the durable checkpoint reached by the migration
                  controller. It is informational; spec.desiredState is the only user input.
                type: string
              source:
                description: |-
                  APIMigrationObjectSnapshot pins the source/target identity and desired spec
                  observed at a durable migration checkpoint.
                properties:
                  specHash:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                type: object
              step:
                description: Step names the current operation within the durable phase.
                type: string
              target:
                description: |-
                  APIMigrationObjectSnapshot pins the source/target identity and desired spec
                  observed at a durable migration checkpoint.
                properties:
                  specHash:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                type: object
              transferredResources:
                description: TransferredResources counts managed objects controlled
                  by the canonical API.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
